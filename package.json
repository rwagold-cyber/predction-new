{
  "name": "predictx",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "chain",
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "deploy:soc": "pnpm --filter chain deploy:soc"
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "picocolors": "^1.1.1"
  }
}
