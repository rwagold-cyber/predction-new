{
  "name": "@predictx/services",
  "version": "1.0.0",
  "description": "PredictX Backend Services (Matcher + Relayer)",
  "scripts": {
    "build": "pnpm -r build",
    "start": "ts-node runner.ts",
    "dev": "ts-node runner.ts",
    "matcher": "cd matcher && pnpm dev",
    "relayer": "cd relayer && pnpm dev"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "ethers": "^6.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "@types/express": "^4.17.21",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2"
  }
}
